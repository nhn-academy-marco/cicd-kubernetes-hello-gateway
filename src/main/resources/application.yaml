spring:
  application:
    name: hello-gateway
  profiles:
    active: local
  lifecycle:
    timeout-per-shutdown-phase: 30s
  config:
    import: classpath:config/actuator.yml
    
server:
  shutdown: graceful



